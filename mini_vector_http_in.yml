sources:
  http_in:
    type: http
    address: "0.0.0.0:19000"
    path: "/ingest"

transforms:
  add_type:
    type: add_field
    field: log_type
    value: linux_syslog

router:
  field: log_type
  routes:
    - equals: linux_syslog
      sink: console

sinks:
  console:
    type: console
